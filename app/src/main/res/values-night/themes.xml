<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Confissões" parent="Theme.Material3.DynamicColors.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red_900</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/red_900</item>
        <item name="colorOnPrimaryContainer">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/red_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/red_700</item>
        <item name="colorOnSecondaryContainer">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorSurface</item>

        <!-- Popup Menu -->
        <item name="textAppearanceLargePopupMenu">@style/TextAppearance.Material3.BodyLarge</item>
        <item name="textAppearanceSmallPopupMenu">@style/TextAppearance.Material3.BodyLarge</item>
        <item name="popupMenuBackground">@drawable/material_popup_background</item>
    </style>
</resources>